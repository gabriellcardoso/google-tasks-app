import * as React from 'react';
import MuiAppBar from 'material-ui/AppBar';

const AppBar = () => (
    <MuiAppBar 
        className="app-bar"
        title="Google Tasks"
    />
);

export { AppBar }